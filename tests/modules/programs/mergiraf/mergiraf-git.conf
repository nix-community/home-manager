[gpg]
	format = "openpgp"

[gpg "openpgp"]
	program = "@gnupg@/bin/gpg"

[merge]
	conflictStyle = "diff3"

[merge "mergiraf"]
	driver = "@mergiraf@/bin/mergiraf merge --git %O %A %B -s %S -x %X -y %Y -p %P -l %L"
	name = "mergiraf"
